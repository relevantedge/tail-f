{
  "name": "@tail-f/client",
  "description": "tail -f client library",
  "license": "MIT",
  "version": "0.8.782",
  "scripts": {
    "build": "rollup --config ../../../build/rollup.client.mjs",
    "build:watch": "pnpm run build -w"
  },
  "dependencies": {
    "@tail-f/types": "workspace:*"
  },
  "publishConfig": {
    "directory": "dist"
  }
}
